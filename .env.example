# Copiar como .env en el servidor y rellenar con valores reales
# chmod 600 .env

# PostgreSQL
DB_USER=sway_app
DB_PASSWORD=sway123
DB_NAME=sway

# Flask
SECRET_KEY=genera_una_clave_con: python3 -c "import secrets; print(secrets.token_hex(32))"

# Email (Gmail SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=tu_correo@gmail.com
MAIL_PASS=tu_app_password_de_16_caracteres

# CORS — IP publica del servidor (sin slash final)
# Ejemplo: CORS_ORIGINS=http://143.110.12.34
CORS_ORIGINS=http://TU_IP_DEL_SERVIDOR
